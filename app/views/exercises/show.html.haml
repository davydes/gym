%h2
  = link_to Exercise.model_name.human(count: ''), exercises_path
  \\
  #{@exercise.name}

  - if can? :manage, @exercise
    .dropdown.pull-right
      %a.btn.btn-sm.btn-primary.dropdown-toggle#dropdownMenu1{type: "button", data:{toggle:"dropdown"}, aria:{haspopup:"true", expanded:"false"}}
        %i.fa.fa-cog
        %span.caret
      %ul.dropdown-menu{aria:{labelledby:"dropdownMenu1"}}
        %li
          = link_to edit_exercise_path(@exercise) do
            %i.fa.fa-edit
            = t 'exercises.actions.edit'
        %li
          = link_to '#', data: { toggle: "modal", target: "#confirm-delete"} do
            %i.fa.fa-trash-o
            = t 'exercises.actions.delete'

%strong #{Exercise.human_attribute_name 'alias'}:
= @exercise.alias.humanize

%br

%strong #{Exercise.human_attribute_name 'body_parts'}:
= @exercise.body_parts.map{ |b| link_to(b.name, body_part_path(b)) }.join(', ').html_safe

%br

%strong #{Exercise.human_attribute_name 'muscles'}:
= @exercise.muscles.map{ |m| link_to(m.name, muscle_path(m)) }.join(', ').html_safe

%br

%strong #{Exercise.human_attribute_name 'description'}:
%br
= @exercise.description

.modal.fade#confirm-delete{tabindex:"-1", role: "dialog", aria: {labelledby: "myModalLabel", hidden: "true"}}
  .modal-dialog
    .modal-content
      .modal-header
        %button{type: "button", class: "close", data: {dismiss: "modal"}, aria: {hidden: "true"}} &times;
        %h4.modal-title#myModalLabel #{t 'exercises.modal_delete.title'}
      .modal-body
        = t 'exercises.modal_delete.body'
      .modal-footer
        %button{type: "button", class: "btn btn-default", data: {dismiss: "modal"}} #{t 'exercises.modal_delete.cancel'}
        = link_to exercise_path(@exercise), class: "btn btn-danger btn-ok", method: 'delete' do
          %i.fa.fa-trash-o
          = t 'exercises.actions.delete'