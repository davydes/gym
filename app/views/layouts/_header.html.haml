%nav.navbar.navbar-default{role: "navigation"}
  .navbar-header
    %button.navbar-toggle{data:{target: '.sidebar-nav', toggle: 'offcanvas'}, type: "button"}
      %span.sr-only Toggle navigation
      %span.icon-bar
      %span.icon-bar
      %span.icon-bar
    %a.navbar-brand{href: root_path}
      Gym
      #{'<sup><strong class="alert-info">dev</strong></sup>'.html_safe if Rails.env.development?}
      #{'<sup><strong class="alert-danger">admin</strong></sup>'.html_safe if admin?}
  .collapse.navbar-collapse
    %ul.nav.navbar-nav.navbar-right
      %li
        - if !current_user
          = link_to new_user_session_path do
            = t 'sessions.sign_in'
            %i.fa.fa-sign-in
        - else
          = link_to destroy_user_session_path, :method => :delete do
            = t 'sessions.sign_out'
            %i.fa.fa-sign-out